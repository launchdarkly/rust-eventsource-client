version: 2
jobs:
  build:
    docker:
      - image: circleci/rust:latest

    steps:
      - checkout

      - run:
          name: Install clippy
          command: rustup component add clippy

      - run: cargo check
      - run: cargo build
      - run: cargo test
      - run: cargo doc --no-deps
      - run: cargo clippy

      - store_artifacts:
          path: target/doc
          destination: doc
